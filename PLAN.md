# 검사 설비 현황 모니터링 시스템

**Capstone 트랙**: 트랙 A — 풀스택 웹앱
**구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 다음 날 발표 전까지 (실질 6~8시간)

## ① 문제 정의 — 지금 무엇이 불편한가
- 누가 / 언제 / 어떤 일에서: 라인 운영자·교대 근무자(및 본인이 가끔)가 2시간 간격으로 9개 라인 × 2대(정면/배면, 총 18개) 설비의 개발 로그를 다운받아 정상/멈춤/알람 여부를 확인하는 일
- 줄이는 비용 (반복 / 대기 / 검증 중 하나): 검증 — 로그를 보고 사람이 정상 여부를 눈으로 판단 → 색상(녹색=정상/노란색=멈춤/빨간색=알람, 알람 우선) UI가 기계적으로 판정
- 지금 걸리는 시간·횟수: 하루 2시간 간격으로 확인, 전 설비 확인에 회당 약 20분

## ② AI 에이전트에게 맡길 부분
- 에이전트가 대신할 일: 샘플 데이터 생성 스크립트 작성, 화면 컴포넌트 코드 작성, 배포 설정 등 구현 전반
- 사람이 계속 할 일: 색상 규칙·화면 레이아웃이 실제로 맞는지 확인하고 방향을 정하는 것 (⑤의 승인 지점 포함)
- 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만): 진행하면서 필요할 때 사용 (서브에이전트, hook, 병렬 작업 후보)

## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
- 반드시 (이게 안 되면 실패):
  1. 9라인 × 2대(정면/배면, 총 18개) 설비 현황을 한 화면에서 색상(녹색=정상/노란색=멈춤/빨간색=알람, 알람 우선)으로 표시
  2. 알람 원인과 조치 이력을 볼 수 있는 이력 리스트 (종합 + 설비별 상세)
  3. 일별/주별 통계 화면(전체·라인별 가동률 그래프+표) — local 환경에서 확인 가능하면 됨
- 되면 좋은: Vercel 배포(실 URL로 접속 가능), 사용자 관리 화면
- 이번엔 안 하는 것: 실제 설비/PLC 연동, 알림 발송(푸시/문자 등), 모바일 대응

## ④ Phase 분할과 각 Phase 완료 기준
1. 샘플 데이터 준비 — 완료: 프로젝트 내 data 생성 폴더에 18개 설비(9라인×정면/배면) 레코드가 생성되고, 각 레코드는 `시간(년/월/일/시/분/초) | 라인 | 설비(정면/배면) | 상태(정상/멈춤/알람) | 원인(멈춤/알람일 때만, 100자 이내) | 조치 상태(알람일 때만: 확인전/확인중/조치완료/조치불가 추가 확인 필요) | 비고(조치 내용, 150자 이내)` 필드를 갖는다. 18개 중 9개는 정상, 나머지는 멈춤·알람이 섞여 있고, 시간은 현재 시각 기준으로 생성된다
2. 대시보드 화면 — 완료: 화면 접속 시 9개 라인이 순서대로 위에서부터 나열되고, 각 라인마다 정면/배면 2개 설비 현황이 상태 색상(녹색/노란색/빨간색, 알람 우선)으로 렌더링된다
3. 종합 이력 리스트 — 완료: 최근 알람 20건이 시간·라인·설비·상태 항목을 포함한 표로 최신순 정렬되어 표시된다
4. 설비별 상세 이력 — 완료: 설비를 클릭하면 알람 발생 시각·원인·조치 내용(샘플 데이터에 미리 포함된 텍스트)이 시간순 리스트로 표시된다
5. 일별/주별 통계 화면 (local 환경) — 완료: 일별/주별 전체 가동률(%)과 라인별 가동률이 구분되어 그래프로 표시되고, 그 아래 상세 표도 함께 표시된다
6. (시간 되면) 배포 — 완료: Vercel에 배포되어 발급된 URL로 접속하면 대시보드가 정상 렌더링된다
7. (시간 되면) 사용자 관리 화면 — 완료: 사용자 목록이 표시되고 추가/삭제가 된다

## ⑤ 완료를 판정할 방법 (검증 게이트)
- 기계가 판정하는 것: 각 Phase의 "완료:" 조건 (④ 참조) — 샘플 데이터 존재·상태값·화면 렌더링·목록 표시 등
- 사람이 눈으로 보는 것: 색상 규칙(녹색/노란색/빨간색, 알람 우선)이 실제로 맞게 표시되는지, 배포된 화면이 실제로 잘 보이는지
- 내가 직접 승인할 지점: 샘플 데이터 구조 확정 후, 대시보드 화면 완성 후, 통계 화면(Phase 5, 필수 범위 끝) 완료 후, 배포(Phase 6) 진행 직전

## GitHub 저장소 URL
https://github.com/hyunduck0/IEMS

## 1:1 코칭에서 가장 묻고 싶은 것
지금 범위는 아니지만, 향후 현황 모니터링에 알람 원인 분석과 조치를 자동으로 처리하는 Agent를 붙이고 싶음.
이때 설비별 PC 환경에 각각 local LLM을 설치해서 처리하는 구조가 맞는지, 아니면 별도 서버에 local LLM을 두고 각 설비의 로그 데이터만 취합해서 전달받아 처리하는 구조가 맞는지 — Agent 아키텍처 설계 방향에 대해 조언을 구하고 싶음.

---
(아래는 폼에 넣지 않는 작업 메모)

## 되돌아갈 지점과 데이터
- 되돌리는 방법: git 저장소 초기화 및 시작 커밋 완료 (`git log`에서 "Start: 검사 설비 현황 모니터링 시스템 기획 계약(PLAN.md)" 커밋으로 되돌리면 됨)
- 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분): 설비 로그는 형식만 같은 샘플 데이터로 생성해서 사용 (실제 설비/사내 시스템 접속 없이 동작)

## 고칠 곳 (4단계 반박 결과)
1. [해결] 배포 여부 불명확 → 배포는 "되면 좋은"으로 유지, Phase 6(시간 되면)으로 분리. 필수 범위는 local 환경까지.
2. [해결] 승인 지점 부족 → 통계 화면(필수 범위 끝) 완료 후, 배포 진행 직전 승인 지점 추가.
3. [해결] Phase 1 완료 기준 모호함 → 필드(시간/라인/설비/상태/원인/조치 상태/비고)와 데이터 개수(18개, 정상 9·멈춤/알람 9)를 구체적으로 확정함.

<!-- 기획 시작 09:52 -->
